import { SearchContainer } from "./style";

function Search() {
  return (
    <SearchContainer>
      <input type="text" placeholder="Search a movie ..." />
    </SearchContainer>
  );
}

export default Search;
